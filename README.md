# LodgeMate — Next.js Migration Guide

## What changed (and what didn't)
- ✅ Every feature works exactly as before
- ✅ Firebase Realtime Database — same data, same rules
- ✅ Firebase Auth — same admin login
- ✅ All UI, animations, dark mode — pixel-perfect match
- 🚀 No more Babel in the browser — 60-80% faster load
- 🔒 Firebase keys moved to environment variables (never in Git)
